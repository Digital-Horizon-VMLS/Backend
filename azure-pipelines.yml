trigger:
- main  # Trigger the pipeline when changes are pushed to the 'main' branch
- develop/vNext # Trigger the pipeline when changes are pushed to the 'development' branch
- personal/alanluu/testing-yaml # Trigger the pipeline when changes are pushed to the 'personal' branch

# Hello world to test automation

pr:
- main  # Trigger the pipeline when pull requests target the 'main' branch
- develop/vNext # Trigger the pipeline when changes are pushed to the 'development' branch
- personal/alanluu/testing-yaml # Trigger the pipeline when changes are pushed to the 'personal' branch

pool:
  vmImage: 'windows-latest'  # You can choose an appropriate VM image

stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - script: echo "Building the application"
    # Add your build steps here

- stage: Test
  jobs:
  - job: Test
    steps:
    - script: echo "Running tests"
    # Add your test steps here